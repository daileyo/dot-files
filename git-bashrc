alias eggit="git config --global -e"
alias egps1="$default_editor /etc/profile.d/git-prompt.sh >/dev/null & "

gws () {
    cd $git_workspace; 
    if [ -z "$1" ]
    then ls;
    else 
        cd ${1:-'.'};
    fi
}

export -f gws;